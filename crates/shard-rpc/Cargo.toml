[package]
name = "shard-rpc"
version = "0.1.0"
edition = "2024"
description = "Versioned wire protocol between shard clients and the shard daemon"
license = "MIT"
authors = ["SwirX <swirxytb@gmail.com>"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"